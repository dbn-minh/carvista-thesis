# car_advisor_chat

**Skill Orchestrator cho AI Agent CarVista — Chatbot tư vấn xe đa lượt.**

## Overview

| Field | Value |
|---|---|
| **Skill name** | `car_advisor_chat` |
| **Version** | 1.0.0 |
| **Complexity** | 🔴 Rất phức tạp (Orchestrator) |
| **Platform** | Antigravity |
| **Scope** | Workspace (`carvista/backend`) |
| **Type** | Orchestrator — gọi các skill con |

## Chức năng

1. **Session Management** — Tạo/load session, merge context, log mọi message
2. **Intent Detection** — Phân loại 6 intent từ câu hỏi user
3. **Entity Resolution** — Tìm variant_id từ tên xe user đề cập
4. **Smart Routing** — Gọi skill con phù hợp với đủ payload
5. **Grounded Answers** — Trả lời chỉ từ DB facts, không bịa

## Skills con được gọi

| Skill | Khi nào | Payload key fields |
|---|---|---|
| `compare_variants` | intent=compare + ≥2 variant resolved | `variant_ids`, `market_id` |
| `predict_price` | intent=predict_price + variant + market | `variant_id`, `market_id`, `horizon_months` |
| `calculate_tco` | intent=tco + base_price + market/profile | `profile_id`, `base_price`, `ownership_years` |

## Cấu trúc thư mục

```
car_advisor_chat/
├── SKILL.md                           ← Instruction chính cho AI Agent
├── README.md                          ← File này
├── examples/
│   ├── example_search.md              ← Case: tìm SUV 7 chỗ
│   ├── example_compare_routing.md     ← Case: routing sang compare_variants
│   └── example_tco_routing.md         ← Case: routing sang calculate_tco
└── resources/
    ├── intent_routing.md              ← Decision tree intent detection
    ├── sql_queries.md                 ← SQL: catalog search + session ops
    └── session_management.md          ← Quy tắc quản lý session + context merge
```

## Database Tables

| Table | Mục đích |
|---|---|
| `ai_chat_sessions` | Lưu session, context, user_id |
| `ai_chat_messages` | Log mọi message (user/assistant/tool) |
| `car_variants` + joins | Search catalog, resolve entity |
| `variant_specs` | Specs cho catalog search |
| `car_reviews` | Rating trong search results |
| `variant_price_history` | Lọc theo giá thị trường |
| `tco_profiles` | Resolve market → profile_id cho TCO |

## Input

```json
{
  "session_id": null,
  "user_id": 42,
  "message": "SUV 7 chỗ dưới 1 tỷ ở VN",
  "context": { "market_id": 1, "budget": 1000000000 }
}
```

## Output

```json
{
  "session_id": 500,
  "answer": "Em tìm thấy 3 SUV 7 chỗ phù hợp ngân sách...",
  "intent": "search_catalog",
  "suggested_actions": [{ "type": "compare_variants", "payload": {...} }],
  "follow_up_questions": ["Anh muốn so sánh các xe này không?"],
  "facts_used": [{ "source": "car_variants", "id": 201 }]
}
```

## Intent Flow

```
User message
    │
    ▼
Intent Detection ──► compare ──────────────► compare_variants skill
    │                predict_price ─────────► predict_price skill
    │                tco ───────────────────► calculate_tco skill
    │                search_catalog ─────────► DB query catalog
    │                buy_sell_guidance ──────► DB query + guidance
    └──────────────► clarification_needed ──► Hỏi lại user
```

## Triggers

- "tư vấn xe", "nên mua xe gì", "xe phù hợp với tôi"
- "SUV 7 chỗ dưới 1 tỷ", "xe điện dưới 800 triệu"
- "so sánh Camry và Accord", "xe nào tốt hơn"
- "tính TCO", "tổng chi phí 5 năm", "chi phí sở hữu xe"
- "giá xe sắp tới", "dự đoán giá Camry 2025"
- Bất kỳ câu hỏi về mua/tìm/so sánh xe ô tô

---

📦 Generated by Skill Generator v3.2
