# Optional: leave unset in local dev to follow the browser hostname automatically.
NEXT_PUBLIC_APP_URL=
# Optional: set this in production when frontend/backend are on different domains.
NEXT_PUBLIC_API_BASE_URL=
# Local dev default when NEXT_PUBLIC_API_BASE_URL is unset.
NEXT_PUBLIC_API_PORT=4000
